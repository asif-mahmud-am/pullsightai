export enum Service {
    PLAN = 'Plan',
    PACK = 'Pack'
}

export enum ServiceBookingRef {
    PURCHASED_PLAN = 'PurchasedPlan',
    PURCHASED_PACK = 'PurchasedPack'
}

export enum Gateway {
    STRIPE = 'stripe',
    PAYPAL = 'paypal'
}
